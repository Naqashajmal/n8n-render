services:
  - type: web
    name: n8n
    env: node
    plan: free
    buildCommand: "npm install -g n8n"
    startCommand: "n8n"
    envVars:
      - key: N8N_PORT
        value: 10000
      - key: WEBHOOK_URL
        value: https://your-app-name.onrender.com
